

# Alterar valor padrão do tipo de chave PIX

## Arquivo único
`src/components/flow/properties/PixProperties.tsx`

## Alteração
Linha 11: substituir `const pixType = data.pixType || "random"` por `const pixType = data.pixType || "phone"`.

