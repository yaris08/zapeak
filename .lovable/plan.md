

# Alterar cor do botão "Testar Fluxo"

## Arquivo único
`src/pages/FlowEditor.tsx`

## Alteração

No componente `CustomControls`, no botão de toggle do simulador, substituir `#f97316` por `#22c55e` na cor quando `showSimulator` é `false`.

A cor vermelha `#ef4444` quando aberto permanece inalterada.

